{% extends 'base.html.twig' %}

{% block title %}Order error{% endblock %}

{% block content %}
    <h2>Order error</h2>
    <p>Welcome <strong>{{ order.user.firstname }} {{ order.user.lastname }}</strong>,<br>
        Your order n°<strong>{{ order.reference }}</strong> had an error.<br>
        A error email is send to your address <strong>{{ order.user.email }}</strong>
    </p>
    <a href="{{ path('order') }}" class="btn btn-success">Retry</a>


{% endblock %}
